import React from 'react';
import ReactDOM from 'react-dom';
import UserRegistrationForm from './UserRegistrationForm';

it('Registration form rendering without crashing', () => {
  // const props = {

  // };
  // UserRegistrationForm(props);
});